import 'package:flutter/material.dart';

class CryptoAppBottomBar extends StatelessWidget {
  const CryptoAppBottomBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return   Container();
  }
}
